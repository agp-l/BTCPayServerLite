RewriteEngine On

# Ignoruj existující soubory (obrázky, CSS, JS) - ty ať se normálně načtou
RewriteCond %{REQUEST_FILENAME} !-f

# Všechno ostatní (včetně existujících složek jako /admin/) pošli do index.php
RewriteRule ^(.*)$ index.php [QSA,L]